PORT=8080
JSON_BODY_LIMIT=250mb
SANDBOX_SLUG_SUFFIX=-sandbox
SANDBOX_SLUG_PREFIX=
SANDBOX_WORKDIR=/root/ai-hub-corporativo/src
# Caminho dentro do contêiner para a chave (volume montado em /run/secrets/openai-token)
OPENAI_API_KEY_FILE=/run/secrets/openai-token/openai_api_key

SANDBOX_KEEP_WORKSPACE=false
