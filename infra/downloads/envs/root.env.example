OPENAI_API_KEY=
# Caminho no host onde o arquivo openai_api_key está salvo. Use ./infra/openai-token (ou outro diretório acessível pelo seu usuário)
# (padrão esperado pelos contêineres) a menos que tenha optado por outro diretório.
OPENAI_TOKEN_HOST_DIR=./infra/openai-token
GITHUB_APP_ID=
GITHUB_PRIVATE_KEY_PEM="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
GITHUB_INSTALLATION_ID=
GITHUB_WEBHOOK_SECRET=
GITHUB_ORG_DEFAULT=
GHCR_REGISTRY=ghcr.io
GHCR_USERNAME=
GHCR_TOKEN=
GHCR_TOKEN_FILE=
# Credenciais do banco de dados principal (MySQL 5.7)
DB_URL=jdbc:mysql://d555d.vps-kinghost.net:3306/aihubcorpdb
DB_USER=aihubcorp_usr
DB_PASS=S3nh@Fort3
# O host d555d.vps-kinghost.net expõe o MySQL com certificado TLS não confiável
# (self-signed). Mantemos sslMode=REQUIRED para criptografar a conexão sem
# validar o certificado. Ajuste para VERIFY_CA/VERIFY_IDENTITY se tiver CA
# confiável configurada no servidor.
DB_SSL_MODE=REQUIRED
DB_ALLOW_PUBLIC_KEY_RETRIEVAL=true
HUB_PUBLIC_URL=http://localhost:8080
SANDBOX_ORCHESTRATOR_HTTP_PORT=8083
SANDBOX_SLUG_PREFIX=
SANDBOX_SLUG_SUFFIX=-sandbox
SANDBOX_TTL_SECONDS=86400
